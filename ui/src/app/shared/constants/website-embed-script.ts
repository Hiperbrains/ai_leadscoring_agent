/** Exact embed snippet for company websites — do not reformat or edit. */
export const WEBSITE_EMBED_SCRIPT = `   <script>
      (() => {
        const API = "https://leadscoring.hiperbrains.com", EVENT_INGEST = "/api/ingest/site-activity", LEAD = "hb_email_click_lead_v3", VIS = "hb_visitor_id_v1", BUF = "hb_visitor_events_v1", ATTR = "hb_visit_attr_v1", GUID = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
        let last = "", identifyPromise = null;
        const j = (v, d = null) => { try { return JSON.parse(v); } catch { return d; } };
        const norm = (v, fb = "website_activity") => (String(v || "").toLowerCase().trim().replace(/[^a-z0-9]+/g, "_").replace(/^_+|_+$/g, "") || fb);
        const evt = (p) => String(p || "").split("?")[0].split("/").filter(Boolean).pop();
        const post = (u, b) => fetch(\`\${API}\${u}\`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(b) });
        const normSrc = (raw) => { if (raw == null) return null; let s = String(raw).trim(); try { s = decodeURIComponent(s); } catch { } s = s.toLowerCase().trim(); if (!s) return null; if (s === "linkdin") s = "linkedin"; if (s === "email" || s === "linkedin" || s === "website" || s === "direct" || s === "organic") return s; return "unknown"; };
        const normCamp = (raw) => { if (raw == null) return null; let s = String(raw).trim(); try { s = decodeURIComponent(s); } catch { } s = s.trim().slice(0, 256); return s.length ? s : null; };
        const normEmail = (raw) => { if (raw == null) return null; let s = String(raw).trim(); try { s = decodeURIComponent(s); } catch { } s = s.trim().toLowerCase(); return s.includes("@") ? s : null; };
        const srcFromParams = (q) => normSrc(q.get("source")) ?? normSrc(q.get("ls_src")) ?? normSrc(q.get("utm_source"));
        const campFromParams = (q) => normCamp(q.get("campaign")) || normCamp(q.get("ls_cmp")) || normCamp(q.get("utm_campaign")) || normCamp(q.get("cmp"));
        const emailFromParams = (q) => normEmail(q.get("email"));
        const getAttr = () => { const o = j(sessionStorage.getItem(ATTR) || ""); return o && typeof o === "object" && typeof o.source === "string" ? o : { source: "website", campaign: null }; };
        const setAttr = (source, campaign) => sessionStorage.setItem(ATTR, JSON.stringify({ source, campaign: campaign || null }));
        const initAttribution = () => { const q = new URLSearchParams(location.search), urlSrc = srcFromParams(q), urlCamp = campFromParams(q), st = j(sessionStorage.getItem(ATTR) || ""), source = urlSrc != null ? urlSrc : (st && typeof st === "object" && typeof st.source === "string" ? st.source : "website"), campaign = urlCamp != null ? urlCamp : (st && st.campaign != null ? st.campaign : null); setAttr(source, campaign); const u = new URL(location.href); let ch = false;["ls_src", "ls_cmp"].forEach((k) => { if (u.searchParams.has(k)) { u.searchParams.delete(k); ch = true; } }); if (ch) history.replaceState(history.state, "", \`\${u.pathname}\${u.search}\${u.hash}\`); };
        const campaign = () => campFromParams(new URLSearchParams(location.search)) || getAttr().campaign || null;
        const newUuid = () => (crypto?.randomUUID?.() ? crypto.randomUUID() : "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, (c) => { const r = (Math.random() * 16) | 0, v = c === "x" ? r : (r & 0x3) | 0x8; return v.toString(16); }));
        const lead = () => { const l = j(sessionStorage.getItem(LEAD) || ""); if (!l) return null; if (l.isUnknownUser) return typeof l.leadId === "string" && typeof l.visitorId === "string" && GUID.test(l.leadId) ? l : null; return GUID.test(l.leadId || "") && typeof l.email === "string" && l.email.includes("@") ? l : null; };
        const leadEmailForPayload = (l) => (l && l.isUnknownUser ? "unknown user" : (l && l.email) || null);
        const visSrc = () => { const l = lead(); return l ? l.source : getAttr().source; };
        const vid = () => sessionStorage.getItem(VIS) || (() => { const v = \`vis_\${(crypto?.randomUUID?.() || \`\${Date.now().toString(36)}_\${Math.random().toString(36).slice(2, 10)}\`)}\`; sessionStorage.setItem(VIS, v); return v; })();
        const be = () => j(sessionStorage.getItem(BUF) || "", []);
        const push = (eventType, metadataJson) => { sessionStorage.setItem(BUF, JSON.stringify([...be(), { eventType, metadataJson, ts: new Date().toISOString() }].slice(-100))); };
        const clear = () => sessionStorage.removeItem(BUF);
        const send = (leadId, eventType, metadataJson) => { const l = lead(); return post(EVENT_INGEST, { visitorId: vid(), leadId: leadId || null, source: visSrc(), campaign: l?.campaign || campaign(), eventType, metadataJson: JSON.stringify(metadataJson) }).then((r) => ({ ok: r.ok, mode: "fetch" })).catch(() => ({ ok: false, mode: "fetch" })); };
        const sendOrBuffer = async (leadId, eventType, metadataJson) => { const result = await send(leadId, eventType, metadataJson); if (!result.ok) { push(eventType, metadataJson); return { ok: false, mode: "buffered" }; } return result; };
        const resolveLeadFromEmailSource = async () => { if (identifyPromise) return identifyPromise; identifyPromise = (async () => { const q = new URLSearchParams(location.search), email = emailFromParams(q), source = srcFromParams(q) || getAttr().source || "website"; if (!email || (source !== "email" && source !== "linkedin")) return lead(); const existing = lead(); if (existing && existing.isUnknownUser && String(existing._resolveEmail || "").toLowerCase() === email) return existing; if (existing && !existing.isUnknownUser && String(existing.email || "").toLowerCase() === email) return existing; const res = await post("/api/leads/identify", { visitorId: vid(), email, source, campaign: campaign() }); let out = null; if (res.ok) { try { out = await res.json(); } catch { out = null; } } const apiLeadId = out && typeof out.leadId === "string" ? out.leadId : ""; if (GUID.test(apiLeadId)) { const l = { leadId: apiLeadId, email, firstEvent: "email_click", source, campaign: campaign(), visitorId: vid() }; sessionStorage.setItem(LEAD, JSON.stringify(l)); return l; } const syntheticLeadId = newUuid(), syntheticVisitorId = newUuid(); sessionStorage.setItem(VIS, syntheticVisitorId); const u = { leadId: syntheticLeadId, isUnknownUser: true, _resolveEmail: email, firstEvent: "unknown_email_link", source, campaign: campaign(), visitorId: syntheticVisitorId }; sessionStorage.setItem(LEAD, JSON.stringify(u)); return u; })().finally(() => { identifyPromise = null; }); return identifyPromise; };
        const page = async (trigger) => { const cur = location.pathname + location.search; if (cur === last) return; last = cur; await resolveLeadFromEmailSource(); const t = norm(evt(location.pathname), "home"), l = lead(), src = visSrc(), camp = campaign(), em = leadEmailForPayload(l); const payload = l ? { action: "page_view", pageType: t, page: location.pathname, url: location.href, source: l.source, campaign: l.campaign || camp, firstEvent: l.firstEvent, ...(em ? { email: em } : {}), trigger } : { action: "page_view", pageType: t, page: location.pathname, url: location.href, source: src, campaign: camp, visitorId: vid(), trigger }; return sendOrBuffer(l?.leadId || null, t, payload); };
        const trackClick = async (eventType = "website_click", metadataJson = {}) => { await resolveLeadFromEmailSource(); const t = norm(eventType, "website_click"), l = lead(), src = visSrc(), camp = campaign(), em = leadEmailForPayload(l); const payload = l ? { ...metadataJson, source: l.source, campaign: l.campaign || camp, firstEvent: l.firstEvent, ...(em ? { email: em } : {}), page: location.pathname, url: location.href } : { ...metadataJson, action: "click", visitorId: vid(), source: src, campaign: camp, page: location.pathname, url: location.href }; return sendOrBuffer(l?.leadId || null, t, payload); };
        const submitWebsiteDemo = async (p = {}) => { const visitorId = vid(), email = String(p.email || "").trim().toLowerCase(); if (!email) throw new Error("email is required"); const l = lead(), src = visSrc(), camp = campaign(); const res = await post("/api/leads/website-demo/submit", { visitorId, email, source: src, campaign: p.campaign || l?.campaign || camp, firstName: p.firstName || "", lastName: p.lastName || "", phoneNumber: p.phoneNumber || "", country: p.country || "", companyName: p.companyName || "", notes: p.notes || "", productId: p.productId ?? 1, metadataJson: JSON.stringify({ ...(typeof p.metadataJson === "object" && p.metadataJson ? p.metadataJson : {}), page: location.pathname, url: location.href, visitorId, bufferedEvents: be() }) }); if (!res.ok) throw new Error("website-demo submit failed"); const out = await res.json(); const nl = { leadId: out.leadId || null, email, firstEvent: "website_demo_submit", source: src, campaign: p.campaign || l?.campaign || camp, visitorId }; if (GUID.test(nl.leadId || "")) sessionStorage.setItem(LEAD, JSON.stringify(nl)); clear(); return out; };
        initAttribution();
        page("initial_load");
        const ps = history.pushState, rs = history.replaceState;
        history.pushState = function () { ps.apply(this, arguments); setTimeout(() => page("pushState"), 0); };
        history.replaceState = function () { rs.apply(this, arguments); setTimeout(() => page("replaceState"), 0); };
        addEventListener("popstate", () => page("popstate"));
        const isPortalSignupAnchor = (a) => { try { const u = new URL(a.href); const path = (u.pathname || "/").replace(/\\/+$/, "") || "/"; return u.hostname.toLowerCase() === "portal.hiperbrains.com" && path === "/home"; } catch { return false; } };
        const portalForwardEmail = () => { const q = new URLSearchParams(location.search), fromUrl = emailFromParams(q); if (fromUrl) return fromUrl; const l = lead(); return normEmail(l?.email || l?._resolveEmail || null); };
        const enrichPortalHref = (href) => { const email = portalForwardEmail(), cmp = campaign(), src = visSrc(); try { const u = new URL(href); if (email && !u.searchParams.has("email")) u.searchParams.set("email", email); if (cmp && !u.searchParams.has("campaign")) u.searchParams.set("campaign", cmp); if (src && !u.searchParams.has("source")) u.searchParams.set("source", src); return u.toString(); } catch { return href; } };
        addEventListener("click", (e) => { const n = e.target?.closest?.("a,button,[data-hb-track-click]"); if (!n) return; const forcedType = n.getAttribute("data-hb-track-click"); const tag = n.tagName?.toLowerCase(); const uiText = (n.textContent || "").trim().slice(0, 120); let eventType = forcedType; if (!eventType && tag === "a") { if (!isPortalSignupAnchor(n)) return; eventType = norm(uiText, "signup_click"); } else if (!eventType) { eventType = norm(uiText, \`\${tag}_click\`); } const meta = { text: uiText, id: n.id || null }; if (tag === "a" && isPortalSignupAnchor(n)) { const enrichedHref = enrichPortalHref(n.href); n.href = enrichedHref; meta.href = enrichedHref; } trackClick(eventType, meta); }, true);
        window.hbTrackDebug = { getSessionLead: () => { const x = j(sessionStorage.getItem(LEAD) || ""); if (!x || typeof x !== "object") return x; const { _resolveEmail, ...rest } = x; return rest; }, clearSessionLead: () => sessionStorage.removeItem(LEAD), getSessionAttribution: getAttr, clearSessionAttribution: () => sessionStorage.removeItem(ATTR), getVisitorId: vid, getBufferedEvents: be, clearBufferedEvents: clear, trackClick, submitWebsiteDemo, resolveLeadFromEmailSource };
      })();
    </script>`;
